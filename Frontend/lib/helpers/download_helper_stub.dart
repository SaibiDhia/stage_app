// lib/helpers/download_helper_stub.dart
// (Stub pour mobile/desktop, ne fait rien)
void downloadBytes(List<int> bytes, String filename) {
  // Optionnel : tu peux afficher un message d'erreur
  // ou rien du tout (c'est safe)
}
